version: 1
coverage:
- behavior.check.combined.both-fail
steps:
- type: run
  argv:
  - conkit
  - check
  - all
  - --source
  - /input/src
  - --contracts
  - /input/contracts
  - --output
  - /output/report.yml
  - --strict
  expect:
    exit_code: 1
    stdout:
      kind: empty
    stderr:
      kind: exact
      value: |
        contract check failed for All
- type: assert_tree
  actual: /output
  expected: output
  contents: text
