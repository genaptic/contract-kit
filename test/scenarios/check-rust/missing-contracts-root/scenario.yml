version: 1
coverage:
- behavior.check.error.contracts-root-missing
steps:
- type: overlay
  source: output/sentinel.txt
  destination: /output/sentinel.txt
- type: run
  argv:
  - conkit
  - check
  - all
  - --source
  - /input/src
  - --contracts
  - /input/missing-contracts
  - --output
  - /output/report.yml
  - --strict
  expect:
    exit_code: 1
    stdout:
      kind: empty
    stderr:
      kind: exact
      value: |
        Contracts root is not a directory: /input/missing-contracts
- type: assert_tree
  actual: /output
  expected: output
  contents: text
