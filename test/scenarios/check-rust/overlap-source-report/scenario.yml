version: 1
coverage:
- behavior.check.error.overlap.source-report
steps:
- type: overlay
  source: output/sentinel.txt
  destination: /output/sentinel.txt
- type: run
  argv:
  - conkit
  - check
  - all
  - --source
  - /input/src
  - --contracts
  - /input/contracts
  - --output
  - /input/src/report.yml
  - --strict
  expect:
    exit_code: 1
    stdout:
      kind: empty
    stderr:
      kind: exact
      value: |
        source path /input/src overlaps report path /input/src/report.yml
- type: assert_tree
  actual: /output
  expected: output
  contents: text
- type: assert_tree
  actual: /input
  expected: input
  contents: bytes
