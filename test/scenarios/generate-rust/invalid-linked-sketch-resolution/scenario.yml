version: 1
coverage:
- behavior.generate.error.invalid-linked-sketch-resolution
- behavior.generate.all.no-partial-write.sketch-domain-failure
steps:
- type: run
  argv:
  - conkit
  - generate
  - signatures
  - --source
  - /input/source
  - --contracts
  - /output/contracts
  - --crate-root
  - scenario=library:nested/lib.rs
  expect:
    exit_code: 0
    stdout:
      kind: exact
      value: |
        signature generation completed: documents 1, signatures 3, preserved sketches 0, semantically changed documents 1, byte-changed documents 1
    stderr:
      kind: empty
- type: overlay
  source: assets/invalid/main.yml
  destination: /output/contracts/main.yml
- type: overlay
  source: assets/invalid/generated-files.json
  destination: /output/contracts/.contract-kit/generated-files.json
- type: run
  argv:
  - conkit
  - generate
  - all
  - --source
  - /input/source
  - --contracts
  - /output/contracts
  expect:
    exit_code: 1
    stdout:
      kind: empty
    stderr:
      kind: exact
      value: |
        failed to parse catalog input main.yml document 0: sketch answer_body code must not be empty
- type: assert_tree
  actual: /output/contracts
  expected: output
  contents: text
