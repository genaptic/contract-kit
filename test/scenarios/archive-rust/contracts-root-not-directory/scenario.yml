version: 1
coverage:
- behavior.archive.error.contracts-root-not-directory
steps:
- type: overlay
  source: output/sentinel.txt
  destination: /output/sentinel.txt
- type: run
  argv:
  - conkit
  - archive
  - --contracts
  - /input/contracts-file
  - --archive
  - /output/archives
  expect:
    exit_code: 1
    stdout:
      kind: empty
    stderr:
      kind: exact
      value: |
        Contracts root is not a directory: /input/contracts-file
- type: assert_tree
  actual: /input
  expected: input
  contents: bytes
- type: assert_tree
  actual: /output
  expected: output
  contents: bytes
