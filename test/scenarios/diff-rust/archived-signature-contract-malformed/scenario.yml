version: 1
coverage:
- behavior.diff.error.archived.signature-contract-malformed
steps:
- type: overlay
  source: output/sentinel.txt
  destination: /output/sentinel.txt
- type: overlay
  source: assets
  destination: /input/archives
- type: run
  argv:
  - conkit
  - diff
  - --contracts
  - /input/contracts
  - --archive
  - /input/archives/malformed/archived-signature.gzip
  expect:
    exit_code: 1
    stdout:
      kind: empty
    stderr:
      kind: exact
      value: |
        failed to diff contracts

        Caused by:
            failed to parse main.yml: signatures: invalid type: string "not-a-list", expected a sequence at line 4 column 13
- type: assert_tree
  actual: /input/archives
  expected: assets
  contents: bytes
- type: remove
  path: /input/archives
- type: assert_tree
  actual: /input
  expected: input
  contents: bytes
- type: assert_tree
  actual: /output
  expected: output
  contents: bytes
