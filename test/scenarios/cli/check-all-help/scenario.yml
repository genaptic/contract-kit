version: 1
coverage:
- grammar.help.check.all
- surface.target.check.all
steps:
- type: run
  argv:
  - conkit
  - check
  - all
  - --help
  expect:
    exit_code: 0
    stdout:
      kind: exact_file
      value: assets/stdout.txt
    stderr:
      kind: empty
