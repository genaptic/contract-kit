version: 1
coverage:
- grammar.help.check.signatures
- surface.target.check.signatures
steps:
- type: run
  argv:
  - conkit
  - check
  - signatures
  - --help
  expect:
    exit_code: 0
    stdout:
      kind: exact_file
      value: assets/stdout.txt
    stderr:
      kind: empty
