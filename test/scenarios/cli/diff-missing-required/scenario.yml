version: 1
coverage:
- grammar.error.diff.missing-required
steps:
- type: run
  argv:
  - conkit
  - diff
  expect:
    exit_code: 2
    stdout:
      kind: empty
    stderr:
      kind: exact_file
      value: assets/stderr.txt
