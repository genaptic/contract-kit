version: 1
coverage:
- grammar.alias.generate.signature
- surface.alias.generate.signature
steps:
- type: run
  argv:
  - conkit
  - generate
  - signature
  - --help
  expect:
    exit_code: 0
    stdout:
      kind: exact_file
      value: assets/stdout.txt
    stderr:
      kind: empty
