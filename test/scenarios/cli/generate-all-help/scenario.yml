version: 1
coverage:
- grammar.help.generate.all
- surface.target.generate.all
steps:
- type: run
  argv:
  - conkit
  - generate
  - all
  - --help
  expect:
    exit_code: 0
    stdout:
      kind: exact_file
      value: assets/stdout.txt
    stderr:
      kind: empty
