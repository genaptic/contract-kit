version: 1
coverage:
- grammar.alias.check.sketch
- surface.alias.check.sketch
steps:
- type: run
  argv:
  - conkit
  - check
  - sketch
  - --help
  expect:
    exit_code: 0
    stdout:
      kind: exact_file
      value: assets/stdout.txt
    stderr:
      kind: empty
