version: 1
coverage:
- grammar.help.generate
- surface.command.generate
steps:
- type: run
  argv:
  - conkit
  - generate
  - --help
  expect:
    exit_code: 0
    stdout:
      kind: exact_file
      value: assets/stdout.txt
    stderr:
      kind: empty
