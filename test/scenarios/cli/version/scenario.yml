version: 1
coverage:
- grammar.version
steps:
- type: run
  argv:
  - conkit
  - --version
  expect:
    exit_code: 0
    stdout:
      kind: exact
      value: |
        conkit 0.1.0
    stderr:
      kind: empty
